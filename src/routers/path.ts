// pathの名前はここで管理する
const mainPath = "/main";

export const pathNames = {
  signIn: "/auth/sign-in",
  signUp: "/auth/sign-up",
  main: mainPath,
  register: `${mainPath}/register`,
  setting: `${mainPath}/setting`,
};
